struct Map {
    layout: Vec<Vec<char>>,
}

impl Map {
    fn load_map(map: &str) -> Self {}

    // TODO: add indexing and all that here.
}

pub fn part1() -> usize {
    let input = include_str!("input").lines().collect::<Vec<_>>();
}
